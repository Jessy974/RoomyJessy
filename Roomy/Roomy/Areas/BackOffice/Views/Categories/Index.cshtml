@model IEnumerable<Roomy.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-responsive table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
        @Html.ActionLink("Details", "Details", new { id=item.ID }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.ID })*@
            <a href="@Url.Action("Edit", new { id=item.ID})"><i class="fa fa-edit text-info"></i></a>
            <a href="@Url.Action("Details", new { id=item.ID})"><i class="fa fa-info-circle text-info"></i></a>
            <a href="@Url.Action("Delete", new { id=item.ID})"><i class="fa fa-trash-o text-info"></i></a>
        </td>
    </tr>
}

</table>
